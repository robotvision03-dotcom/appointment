; Example Asterisk dialplan for an Iranian SIP trunk (not Twilio).
; After your own ASR fills ${UTTERANCE}, POST it to the agent and Playback the WAV.
;
; [clinic-ai]
; exten => s,1,Answer()
; same => n,Set(SESSION=sip-${UNIQUEID})
; same => n,System(curl -s -o /tmp/${SESSION}.json -H 'Content-Type: application/json' -d '{"session_id":"${SESSION}","phone":"${CALLERID(num)}","text":""}' http://127.0.0.1:38471/sip/turn)
; same => n,Playback(/tmp/greeting)  ; generate via GET /api/tts
; same => n,Hangup()

[clinic-ai]
exten => s,1,NoOp(Persian clinic AI — wire ASR then POST /sip/turn)
